name: services
description: A very good dart package
version: 0.1.0+1
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^2.14.0
  data:
    path: ../data
  repositories:
    path: ../repositories

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.2
  very_good_analysis: ^5.1.0
